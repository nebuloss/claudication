{
  "name": "claudication-admin",
  "version": "0.1.0",
  "private": true,
  "description": "Admin UI for claudication: React and Tailwind, compiled into the static Go binary that serves it.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "//engines": "The UI is compiled into the binary, so its output is part of a reproducible build. The lockfile pins the compilers; this pins the runtime they execute on. Node 20 is the floor Vite 6 supports and what the build machine runs.",
  "engines": {
    "node": ">=20.19"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^20.14.2",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.4.5",
    "vite": "^6.0.0"
  }
}
